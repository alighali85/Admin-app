// create Initial staet to  statrt the application with 

const initialState = {
    auth: false,
    userId: '0',
    token: '0'
}

// create the reducers 
export default( state = initialState, action )=> {
    return initialState
}